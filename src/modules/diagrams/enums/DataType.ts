// https://www.w3schools.com/sql/sql_datatypes.asp
// MYSQL types
export enum DataType {
    // STRING
    varchar = 'varchar',
    text = 'text',

    // NUMERIC
    bit = 'bit',
    tinyint = 'tinyint',
    int = 'int',
    bigint = 'bigint',
    float = 'float',
    double = 'double',

    //DATE
    date='date',
    datetime='datetime',
    timestamp='timestamp'
}
